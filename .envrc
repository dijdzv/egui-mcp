#!/usr/bin/env bash
# devenv + direnv 統合
# ディレクトリに入ると自動で開発環境がロードされる

if ! has devenv; then
  log_error "devenv がインストールされていません"
  log_error "インストール: https://devenv.sh/getting-started/"
  return 1
fi

eval "$(devenv direnvrc)"
use devenv
